const test=require('node:test');
const assert=require('node:assert/strict');
const fs=require('node:fs');
const path=require('node:path');
const root=path.resolve(__dirname,'..');
const read=p=>fs.readFileSync(path.join(root,p),'utf8');
const hair=read('www/chisel-tryon-hair-v5.js');
const hairPack=read('android/app/src/main/assets/public/chisel-tryon-hair-v5.js');
const beard=read('www/chisel-beard-tuning-v5.js');
const beardPack=read('android/app/src/main/assets/public/chisel-beard-tuning-v5.js');
const loader=read('www/chisel-ar-coach-core.js');
const loaderPack=read('android/app/src/main/assets/public/chisel-ar-coach-core.js');
const v4=read('www/chisel-tryon-runtime-fixes.js');
const workflow=read('../.github/workflows/chisel-tests.yml');

test('V5 try-on modules are synchronized into Android',()=>{assert.equal(hairPack,hair);assert.equal(beardPack,beard);assert.equal(loaderPack,loader);});
test('V5 hair replaces the old overlay and is loaded after V4',()=>{assert.match(loader,/chisel-tryon-hair-v5\.js/);assert.match(loader,/ChiselTryonHairV5\.install/);assert.match(hair,/drawHair\.__v5b=true/);assert.doesNotMatch(hair,/old\(P,c,h\)/);});
test('men hair silhouettes have strong geometry separation',()=>{assert.match(hair,/buzz:\[\.055,\.025,0,\.92/);assert.match(hair,/crop:\[\.12,\.045,0,\.98/);assert.match(hair,/quiff:\[\.35,\.075,0,1\.05/);assert.match(hair,/mlong:\[\.23,\.13,\.68,1\.09/);assert.match(hair,/s\.id==='crop'/);});
test('women hair lengths and widths are visibly separated',()=>{assert.match(hair,/frenchbob:\[\.20,\.13,\.48,1\.08/);assert.match(hair,/butterfly:\[\.31,\.18,1\.08,1\.15/);assert.match(hair,/curls:\[\.35,\.24,\.90,1\.20/);assert.match(hair,/coils:\[\.38,\.27,\.82,1\.24/);});
test('beard tuning favors smooth mass and micro-length texture with clear progression',()=>{assert.match(beard,/stubble:\[\.20,\.10,320,\.0010\]/);assert.match(beard,/short:\[\.34,\.14,500,\.0017\]/);assert.match(beard,/full:\[\.46,\.18,700,\.0025\]/);assert.match(beard,/drawBeard\.__v5b=true/);assert.match(loader,/chisel-beard-tuning-v5\.js/);});
test('V4 selection sync and clean beard default remain underneath V5',()=>{assert.match(v4,/setTimeout\(syncPreviewSummary,180\)/);assert.match(v4,/styleBeard=0/);});
test('fake camera generation remains bounded',()=>{const matches=workflow.match(/-frames:v 60/g)||[];assert.ok(matches.length>=2);});
