const test=require('node:test'),assert=require('node:assert/strict'),fs=require('node:fs'),path=require('node:path');
const root=path.resolve(__dirname,'../..'),www=path.join(__dirname,'../www');
const assets=['chisel-ar-coach-core.js','chisel-looks-core.js','chisel-looks-studio.js','chisel-policy-shared.js','chisel-comparison-shared.js','chisel-wallet-core.js','chisel-credits-ui.js','chisel-compare-tools.js','chisel-pro-tools.css'];
test('credit app assets match the packaged Android copies',()=>{for(const name of assets)assert.equal(fs.readFileSync(path.join(www,name),'utf8'),fs.readFileSync(path.join(__dirname,'../android/app/src/main/assets/public',name),'utf8'),name);});
test('client and server tariffs and comparison rules share the same source',()=>{for(const [name,global,exposed]of [['policy','ChiselCreditPolicy','VERSION,PACKS,MODES,MODEL,UUID,mode,contribution,purchaseEvent,usageMicros'],['comparison','ChiselComparePolicy','pickLooks,briefHTML']]){const s=fs.readFileSync(path.join(root,'supabase/functions/credit-studio',name+'.mjs'),'utf8').replace(/export /g,'');const out=fs.readFileSync(path.join(www,'chisel-'+name+'-shared.js'),'utf8');assert.ok(out.includes(s.trim()),name);assert.ok(out.includes(global));}});
test('ledger tariffs match storefront packs and default to no sale or render',()=>{const sql=fs.readFileSync(path.join(root,'supabase/migrations/20260910230000_credit_wallet.sql'),'utf8');for(const s of ["('chisel_credits_10',10,399)","('chisel_credits_30',30,999)","('chisel_credits_70',70,1999)","('standard',1,130000)","('detail',3,390000)"])assert.ok(sql.includes(s));assert.match(sql,/sales_enabled boolean not null default false/);assert.match(sql,/renders_enabled boolean not null default false/);});
