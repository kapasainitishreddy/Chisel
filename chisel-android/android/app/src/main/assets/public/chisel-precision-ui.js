(function attachChiselPrecisionUI(root,factory){const core=root&&root.ChiselPrecisionCore?root.ChiselPrecisionCore:(typeof require==='function'?require('./chisel-precision-core.js'):null);const api=factory(root||globalThis,core);if(typeof module==='object'&&module.exports)module.exports=api;if(root)root.ChiselPrecisionUI=api;})(typeof globalThis!=='undefined'?globalThis:this,function createPrecisionUI(root,core){
  'use strict';if(!core)throw new Error('ChiselPrecisionCore is required.');const STORAGE_KEY='chisel:precision:v2',MIN_FACE_FRAMES=7,MIN_BODY_FRAMES=3;
  const escape=(v)=>String(v==null?'':v).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;').replace(/'/g,'&#039;');
  function readStore(){const fallback={version:2,faceScans:[],bodyScans:[],calibration:null,settings:{}};try{const p=JSON.parse(root.localStorage.getItem(STORAGE_KEY)||'null');return p&&typeof p==='object'?{...fallback,...p,version:2}:fallback;}catch(_){return fallback;}}
  function writeStore(next){const value={...next,version:2};try{root.localStorage.setItem(STORAGE_KEY,JSON.stringify(value));}catch(_){}return value;}
  const protocolList=(items)=>`<ul class="chp-protocol">${items.map((item)=>`<li>${escape(item)}</li>`).join('')}</ul>`;
  function buildShell(){return `<section id="chiselPrecisionRoot" class="chp-root" hidden aria-hidden="true"><div class="chp-backdrop" data-chp-close></div><div class="chp-dialog" role="dialog" aria-modal="true" aria-labelledby="chp-title"><header class="chp-header"><div><span class="chp-kicker">STRICT REPEATABILITY PROTOCOL</span><h2 id="chp-title">Precision Mode</h2></div><button class="chp-close" type="button" data-chp-close aria-label="Close Precision Mode">×</button></header><div class="chp-truth"><b>90+ means capture quality, not laboratory accuracy.</b><span>Chisel saves a precision result only when multiple photos agree, setup conditions match, and uncertainty is low. Weak evidence returns no result.</span></div><nav class="chp-tabs" aria-label="Precision tools"><button class="is-active" type="button" data-chp-tab="face">Face & skin</button><button type="button" data-chp-tab="body">Body & waist</button><button type="button" data-chp-tab="progress">Progress</button></nav><main class="chp-main">${facePanel()}${bodyPanel()}${progressPanel()}</main></div></section>`;}
  function facePanel(){return `<section class="chp-panel is-active" data-chp-panel="face"><div class="chp-panel-head"><span class="chp-kicker">7–12 MATCHED PHOTOS</span><h3>Face, skin, lips, cheekbones & bloat</h3><p>Use burst photos from one session. Precision Mode fuses stable frames and reports confidence intervals instead of one fragile number.</p></div>${protocolList(['Same front camera, distance, zoom and portrait orientation.','Neutral expression; remove makeup for skin tracking.','One diffuse light source in front; disable beauty filters and portrait effects.','Keep the full face and upper neck visible; no landmark occlusion.'])}<div class="chp-upload-grid"><label class="chp-upload"><span>Select ${MIN_FACE_FRAMES}–12 neutral photos</span><input id="chp-face-files" type="file" accept="image/*" multiple><small>Take a burst and select the clearest sequence.</small></label><label class="chp-upload"><span>Optional ${MIN_FACE_FRAMES}–12 open-mouth photos</span><input id="chp-open-files" type="file" accept="image/*" multiple><small>Same setup, natural opening. Enables expression remapping.</small></label></div><button id="chp-run-face" class="chp-primary" type="button">Run precision face batch</button><div id="chp-face-status" class="chp-status" aria-live="polite"></div><div id="chp-face-result" class="chp-result"></div></section>`;}
  function bodyPanel(){return `<section class="chp-panel" data-chp-panel="body" hidden><div class="chp-panel-head"><span class="chp-kicker">FRONT + SIDE CONSENSUS</span><h3>Body, posture & waist tracking</h3><p>Three or more front and side photos are fused using the full pose model and person segmentation. Hidden silhouettes return no result.</p></div>${protocolList(['Rear camera at waist height, 1× zoom, fixed position and plain contrasting background.','Fitted clothing; feet visible; arms slightly away from the torso.','Front: straight-on. Side: true 90° stance.','Enter one real tape measurement only to calibrate personal relative trends.'])}<div class="chp-upload-grid"><label class="chp-upload"><span>Front photos · minimum ${MIN_BODY_FRAMES}</span><input id="chp-body-front" type="file" accept="image/*" multiple></label><label class="chp-upload"><span>Side photos · minimum ${MIN_BODY_FRAMES}</span><input id="chp-body-side" type="file" accept="image/*" multiple></label></div><label class="chp-field"><span>Optional real waist tape baseline</span><div><input id="chp-waist-cm" type="number" min="30" max="250" step="0.1" inputmode="decimal" placeholder="e.g. 78.0"><em>cm</em></div><small>Measure at the same anatomical line used by the photos.</small></label><button id="chp-run-body" class="chp-primary" type="button">Run precision body batch</button><div id="chp-body-status" class="chp-status" aria-live="polite"></div><div id="chp-body-result" class="chp-result"></div></section>`;}
  function progressPanel(){return `<section class="chp-panel" data-chp-panel="progress" hidden><div class="chp-panel-head"><span class="chp-kicker">UNCERTAINTY-AWARE HISTORY</span><h3>Only compare compatible scans</h3><p>A change appears only when it exceeds both scans' uncertainty and the camera, framing, distance and lighting remain compatible.</p></div><div id="chp-progress-result" class="chp-result"></div><button id="chp-clear" class="chp-secondary" type="button">Clear Precision Mode history</button></section>`;}
  const format=(r,suffix='')=>!r||!r.valid||r.value==null?'—':`${r.value}${suffix} ± ${r.ci95}${suffix}`;
  const metric=(label,value,note)=>`<div class="chp-metric"><span>${escape(label)}</span><b>${escape(value)}</b>${note?`<small>${escape(note)}</small>`:''}</div>`;
  const quality=(score,accepted)=>`<div class="chp-quality${accepted?' is-pass':' is-retry'}"><strong>${score}/100</strong><span>${escape(core.precisionLabel(score,accepted))}</span></div>`;
  const reasons=(items)=>items&&items.length?`<div class="chp-reasons"><h4>Why this batch was not saved</h4><ul>${items.map((x)=>`<li>${escape(x)}</li>`).join('')}</ul></div>`:'';
  function faceResult(face,skin,expression,failures){const accepted=face.accepted&&skin.accepted,score=Math.min(face.score,skin.score);return{accepted,score,html:`${quality(score,accepted)}<div class="chp-metrics">${metric('Cheekbone ratio',format(face.metrics.cheekboneWidth))}${metric('Jaw ratio',format(face.metrics.jawWidth))}${metric('Gonial angle',format(face.metrics.gonialAngle,'°'))}${metric('Lip fullness',format(face.metrics.lipFullness))}${metric('Eye spacing',format(face.metrics.eyeSpacing))}${metric('Canthal tilt',format(face.metrics.canthalTilt,'°'))}${metric('Brow arch',format(face.metrics.browArch))}${metric('Bloat/fullness',format(face.metrics.bloatFullness))}${metric('Skin redness',format(skin.metrics.redness))}${metric('Skin evenness',format(skin.metrics.evenness))}${metric('Blemish signal',format(skin.metrics.blemish))}${metric('Neck redness',format(skin.metrics.neckRedness))}${metric('Neck evenness',format(skin.metrics.neckEvenness))}${metric('Lip colour Lab',skin.lipLab?skin.lipLab.map((v)=>Number(v).toFixed(1)).join(' · '):'—')}${metric('Colour repeatability',skin.medianDeltaE==null?'—':`ΔE ${skin.medianDeltaE}`,'lower is more repeatable')}</div>${expression?`<div class="chp-history-card"><h4>Open-mouth remap · ${expression.score}/100</h4><p>${expression.accepted?`Corrected cheekbone ${format(expression.corrected.cheekboneWidth)} · jaw ${format(expression.corrected.jawWidth)} · gonial ${format(expression.corrected.gonialAngle,'°')}`:'Open-mouth batch was not stable enough to save.'}</p></div>`:''}<p class="chp-note">Intervals describe within-batch agreement, not medical or laboratory accuracy.</p>${failures.length?`<p class="chp-note">${failures.length} file(s) could not be used.</p>`:''}${reasons([...face.reasons,...skin.reasons,...(expression?expression.reasons:[])])}`};}
  function bodyResult(result,failures){const r=result.ratios||{};return{accepted:result.accepted,score:result.score,html:`${quality(result.score,result.accepted)}<div class="chp-metrics">${metric('Waist : hip perimeter',r.waistToHip==null?'—':r.waistToHip.toFixed(4))}${metric('Shoulder : waist width',r.shoulderToWaist==null?'—':r.shoulderToWaist.toFixed(4))}${metric('Photo waist perimeter',r.photoWaistPerimeter==null?'—':`${r.photoWaistPerimeter.toFixed(5)} ± ${r.photoWaistPerimeterCi95.toFixed(5)}`,'normalized trend')}${metric('Calibrated waist',result.calibratedWaistCm==null?'Not calibrated':`${result.calibratedWaistCm.toFixed(1)} cm`,'relative to tape baseline')}${metric('Waist width',format(result.metrics.waistWidth))}${metric('Waist depth',format(result.metrics.waistDepth))}${metric('Hip width',format(result.metrics.hipWidth))}${metric('Hip depth',format(result.metrics.hipDepth))}${metric('Neck posture angle',format(result.metrics.neckAngle,'°'),'ear–shoulder proxy')}</div><p class="chp-note">Centimetre output is a relative calibrated trend, not a replacement for a tape measure.</p>${failures.length?`<p class="chp-note">${failures.length} file(s) could not be used.</p>`:''}${reasons(result.reasons)}`};}
  return{STORAGE_KEY,MIN_FACE_FRAMES,MIN_BODY_FRAMES,escape,readStore,writeStore,buildShell,format,metric,faceResult,bodyResult};
});
